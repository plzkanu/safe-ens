{
  "name": "safe-ens",
  "version": "1.0.0",
  "description": "안전순찰일지 및 SAO 전산화 시스템",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm start",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "build:all": "cd frontend && npm run build && cd ../backend && npm run build",
    "start:replit": "npm run setup:replit && npm run serve:replit",
    "setup:replit": "npm install && cd backend && npm install && npx prisma generate && cd ../frontend && npm install && npm run build",
    "serve:replit": "cd backend && node dist/server.js"
  },
  "keywords": [
    "safety",
    "patrol",
    "sao",
    "inspection"
  ],
  "author": "",
  "license": "UNLICENSED"
}
